# animals-audio

C# cross-platform audio parsing library and tools, including tests, in dotnet `sdk`. The original CSCore was languishing in Framework and unusable for modern contexts. Note that the tests project does not fully compile, given a lot of pollution from Framework Windows.
